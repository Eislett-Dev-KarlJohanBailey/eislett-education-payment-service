{
    "private": true,
    "workspaces": [
      "services/*",
      "libs/*"
    ],
    "scripts": {
      "build": "turbo run build",
      "package": "turbo run package",
      "deploy": "turbo run deploy"
    },
    "devDependencies": {
      "turbo": "^1.13.0",
      "typescript": "^5.3.0"
    }
  }
  