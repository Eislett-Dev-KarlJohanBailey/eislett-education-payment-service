{
  "name": "@services/entitlement-processor-service",
  "version": "1.0.0",
  "description": "",
  "main": "dist/handler/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "package": "esbuild src/handler/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js && zip -j function.zip dist/index.js",
    "deploy": "echo Deploying entitlement-processor-service..."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.975.0",
    "@aws-sdk/lib-dynamodb": "^3.975.0",
    "@aws-sdk/client-sns": "^3.975.0",
    "@libs/domain": "*",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.27.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.160",
    "@types/node": "^25.0.10",
    "typescript": "^5.9.3"
  }
}
